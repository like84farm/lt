{
  "name": "gpt-chat-web",
  "version": "1.0.0",
  "private": true,
  "description": "A simple GPT-compatible chat webpage with local conversation history.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
